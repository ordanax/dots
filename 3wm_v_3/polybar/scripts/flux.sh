#!/bin/sh
if [ click-left ]; then
        killall -s 6 xflux && xflux -l 56.34, -g 18.95 -k 2000
if [ click-right ]; then
        killall -s 6 xflux && xflux -l 56.34, -g 18.95 -k 2000
    else
        echo "пипа";
    fi